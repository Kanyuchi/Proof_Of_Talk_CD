# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Node
node_modules/
.next/

# Environment
.env
.env.local
.env.production

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# AWS
.aws/

# Build outputs
*.egg
*.whl
~$*.docx
~$*.xlsx

# Local Netlify folder
.netlify
.gstack/

# Data exports and PhantomBuster results (local only)
csv/
exports/*.csv
exports/*.xlsx

# Launch video binaries (the renderer needs them at runtime, but they're heavy and Sithum-supplied)
launch/from_sithum/
launch/our_version/*.mp3
!launch/our_version/voiceover.mp3
launch/our_version/*.png
launch/our_version/figures/
launch/our_version/.DS_Store
launch/our_version/pot_matchmaker_*.mp4
launch/our_version/.render_frames/

# WhatsApp Zohair bridge — never commit credentials or message data
tools/whatsapp-mcp/whatsapp-bridge/store/
tools/whatsapp-mcp/.env
tools/whatsapp-mcp/**/*.db
tools/whatsapp-mcp/**/*.db-journal

# Playwright MCP browser artifacts (accessibility snapshots, screenshots, console logs)
.playwright-mcp/

# Root-level scratch screenshots (scene*.png, preview-*.png, email-*.png, dashboard-*.png, etc.)
# Already-tracked root PNGs stay tracked; this only blocks new clutter.
/*.png

# Backend operational exports + ledgers (local runtime, not source).
# The 5 already-tracked files here stay tracked; this only blocks new junk.
backend/exports/

# Local-only / vendored content — triaged 2026-05-22, not part of source.
# (Matchmaking_Feedback/ keeps its 12 tracked files; only its stray PDF is ignored.)
.claude/
Logo/
mobile-audit/
launch/from_Z/
launch/our_version.zip
backend/scripts/diagnose_*.py
backend/scripts/investigate_*.py
docs/superpowers/plans/2026-05-21-deeper-match-pool.md
tools/whatsapp-mcp/LICENSE
tools/whatsapp-mcp/.gitignore
tools/whatsapp-mcp/example-use.png
tools/whatsapp-mcp/whatsapp-bridge/whatsapp-client
Matchmaking_Feedback/Proof of Talk 2026*.pdf
