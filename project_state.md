# Project State — POT Matchmaker

**Last updated:** 2026-05-29 17:50 — **Match-intro digest cron shipped (kill-switch OFF).** Closes the "match-intro fires once per attendee lifetime" gap. New 09:00 UTC cron `_daily_match_digest` sends "N new top matches for Proof of Talk 2026" to existing attendees whose curated/priority_intro pool gained ≥3 new matches since their `last_match_digest_at`. Per-attendee 72h throttle; per-run safety cap 200; `_naive()` helper handles Supabase's tz-aware `Match.created_at` correctly (pinned by regression test — would have silently 100%-errored without it). Wired with `MATCH_DIGEST_ENABLED=false` default (mirror of `RECIPROCITY_NOTIFY_ENABLED`) so a parallel `full_refresh_matches.py` (PID 27887, ETA ~01:30 UTC) can't accidentally fire-everyone tomorrow morning. Migration `a7b8c9d0e1f2` nullable col; 18 new tests; suite 417 passed. **To enable:** wait for refresh → re-bootstrap → flip Railway env flag. See whats_next `[match-digest-enable-after-refresh]`.

**Prior (2026-05-29):** — **Adoption "Active" counter no longer overshoots Accounts.** The dashboard's `cumulative_active` and `active_last_7d` now require the active attendee to have a real `users` row (non-admin, non-demo) — via a new `_account_linked_filter()` EXISTS subquery in `usage_snapshot.py`, applied additively to both the 03:45 UTC cron and the live `GET /dashboard/adoption` endpoint. `magic_link_active` (the "Magic-link opens: 425" engagement card) is intentionally left unchanged so unregistered openers stay visible. Prior to this fix, 132 directory rows that received the 2026-05-28/29 welcome blasts but never registered were inflating Active above total accounts (438 > 391). Simulated post-fix: 307 active vs 392 accounts. Not a regression of PR #11 — that fix moved Active from the lagging snapshot to a live query, but kept the original "any `last_seen_at` counts" rule which only just stopped holding. 370 tests pass.

**Prior (2026-05-28):** — **Automated emails unblocked: `EMAIL_MODE=all` staged on Railway + morning-of cron (Phase 5) implemented.** The two request-path emails that were team-only since EMAIL_MODE shipped are now live for all recipients on the next deploy: `send_match_intro_email` (per attendee on `generate_matches_for_attendee`, fires from the 03:30 UTC `_daily_match_refresh` for net-new only — bulk-rebuild blast guard since 2026-05-21 keeps `generate_all_matches` `notify=False`) + `send_meeting_confirmation_email` (per side per `PATCH /schedule` booking). `send_morning_schedule_email` Phase 5 stub implemented end-to-end: HTML/text body via the shared `_render_email` Louvre shell, per-meeting cream cards with time/name/company/location, `force=True` for off-request-path send. New `app/services/morning_schedule.py` queries mutually-accepted matches with `meeting_time` on the conference day (status_a='accepted' AND status_b='accepted'), groups per attendee, skips opt-outs, sends both sides. `CronTrigger(hour=7, timezone="Europe/Paris")` wired year-round; the service short-circuits on non-conference days so leaving the trigger live is safe (no-op except 2026-06-02 / 06-03). 9 new tests; suite 318 passed. Kill switch for #1+#2: flip `EMAIL_MODE=allowlist` (no redeploy). Phase 5/6 still empty: `send_post_event_wrapup_email` (D+1) + `send_followup_nudge_email` (D+7).

**Prior (2026-05-25):** — **Reciprocity loop shipped (PR #9 + #10) and 2-hourly notify cron live (`RECIPROCITY_NOTIFY_ENABLED=true` activated on Railway).** New `app/services/checkins_sync.py` ingests the per-attendee Extasy *checkins* report to recover people the buyer-keyed orders/tickets feed collapses (multi-pass orders) or misses (no-email passes) — closing part of the 945-vs-1065 matchmaker-vs-dashboard gap. Pass type via order#+QR join (100% resolve); existing-wins backfill of blank company/title/country; new rows auto-enrich. Admin `POST /dashboard/sync-checkins` + daily 02:05 UTC cron + `checkins_sync` heartbeat. Shipped commit `41891f9`; prod backfill ran (heartbeat inserted=44/backfilled=338/upgraded=1/0 errors); **live API total 945 → 993**. **NB:** the admin sync endpoint 504s at the ~28s gateway on large runs but the handler completes server-side; and the case-sensitive email match (shared with extasy_sync) can create dup-person rows (pre-existing Sithum Akalanka dup) — both logged as follow-ups in whats_next + session_log. Verified: 249 pytest pass + live dry-run (rolled back) before deploy. Spec `docs/superpowers/specs/2026-05-25-extasy-checkins-sync-design.md`.

**Prior (2026-05-24):** **Adoption & usage tracking shipped live (PR #7) + LinkedIn-pending banner fixed.** PR #7 rebased→migrated(prod)→merged→deployed→smoke-tested end-to-end; the recommended admin/demo exclusion from active metrics is implemented + verified live (no open items). Separately, the dashboard "pending LinkedIn enrichment" banner was corrected from an inflated 91 to a true ~10 (`_linkedin_pending` excludes dead/empty-stub/demo rows). Details in the What's Working entries below + session_log.

**Prior (2026-05-23):** **Welcome rollout: no-token gap closed + final 63-wave sent; pool drained.** Directory now **902 attendees** (was 737 on 2026-05-21, ~165 new since). Ran `scripts/backfill_magic_tokens.py` (62 NULL tokens → 0 no-token). The backfill momentarily made the 7 `@demo.proofoftalk.io` video-demo personas (created 2026-05-22) eligible; set `email_opt_out=true` on all 7 so they can never receive real mail (would hard-bounce, dents sender reputation) — doesn't touch their concierge-demo isolation. Sent **one wave of 63 real attendees** from warm `team@xventures.de` — **sent=63 / failed=0** (all Resend 200/201), under the ~100/day ceiling, 0 sent earlier today. **Welcome ledger now 879; eligible pool drained to 0** (7 demos opted-out, 16 gated speakers await consent, 879 sent = 902 total). Earlier 05-21/05-22 waves (411 + 405) had blown ~4× past the ~100/day ceiling two days running; today's single 63 stays within it.

**Prior (2026-05-21):** **Launch readiness: welcome rollout unblocked + DB hardened.** Going-live audit found two blockers, both now cleared. (1) **Magic tokens**: only 43/737 attendees had a `magic_access_token`, so the welcome email's "Open your matches" link would dead-end 94% of people at the login wall. Ran `scripts/backfill_magic_tokens.py` (mirrors `POST /matches/generate-tokens`) → **737/737 covered, 721 welcome-eligible** (16 gated speakers excluded). (2) **DB connection**: `app/core/database.py` hardened — `pool_pre_ping=True` + `pool_recycle=1800` (fixes intermittent 500s from Supabase dropping idle connections), env-tunable `pool_size`/`max_overflow` (`DB_POOL_SIZE`/`DB_MAX_OVERFLOW`, defaults 5/10), and auto `statement_cache_size=0` when `DATABASE_URL` is the `:6543` pooler — so flipping to the transaction-mode pooler for spike capacity is now a safe one-line env change. **New staged welcome sender** `scripts/send_welcome_batch.py`: preview-by-default, `--confirm` to send, `--limit` waves, `--only` smoke test, `--status`; excludes opted-out/gated/tokenless rows; ledger `exports/welcome_sent.log` prevents double-sends; uses `send_welcome_email(force=True)` to bypass the `EMAIL_MODE` gate for a deliberate batch while automated triggers stay gated on `allowlist`. **Welcome waves in progress (2026-05-21):** ledger **408 sent, 315 eligible remain**; **411 sent today** (261 earlier/parallel + a 150 operator-override wave this session) from warm `team@xventures.de` — over the self-imposed ~100/day and Zohair's ~150/day ceiling, so welcome sends should pause until 2026-05-22 then resume ~100/day. **Still operator's call:** optional pooler flip and `EMAIL_MODE=all`. Commits 927bc86 + 53367ca.

**Prior (2026-05-20, late):** **Email templates redesigned to match the newsletter + speaker consent gate shipped.** EMAIL: all 5 emails (reset, match-intro, mutual-match, meeting-confirmation, new welcome) now share the `_render_email` wrapper styled to the PoT newsletter — Louvre header/footer banners, Playfair Display + Poppins (Media Kit fonts, embedded via Google Fonts with web-safe fallback), terracotta `#C2632A` CTA, real LinkedIn/X icons, Unsubscribe·Preferences, no em dashes. Assets in `frontend/public/email/` (served at meet.proofoftalk.io/email/*). `EMAIL_MODE=allowlist` so only team receives. Open: welcome-email trigger, `matchmaker@xventures.de` receiving mailbox, RESEND_FROM_EMAIL switch, real unsubscribe backend, EMAIL_MODE=all rollout. Commits 8824075 + ddf77a4. — **High-profile speaker consent gate shipped.** 17 confirmed speakers flagged orange (`#f9cb9c`) in the "2026 - Confirmed Speakers" sheet must not appear in matching until they consent. New `attendees.matching_consent` column (`not_required`/`pending`/`granted`/`declined`); `consent_filter.is_match_gated()` excludes `pending`+`declined` at both matching boundaries (mirrors `staff_filter`); `seed_speaker_consent.py` + `set_speaker_consent.py` scripts. Live run set 16 to `pending` (Tom Lee not in DB), scrubbed 128 matches, verified 0 gated speakers remain matched — closed the exposure where 8 (Aave, Franklin Templeton, Robinhood, Blockstream, State Street, etc.) were live in others' matches. Consent granted manually as Sneha confirms. **Also discovered: the speaker sync reads the wrong sheet tab ("OLD LIST OF SPEAKER" — no gid on the CSV export), missing the rich "2026 Form responses" tab (real emails, LinkedIn, images); reconciliation is a deferred follow-up.** Code on origin/main (deploys when Railway incident clears; data scrub already protects regardless). Spec/plan: `docs/superpowers/{specs,plans}/2026-05-20-speaker-consent-gate*`.

**Prior (2026-05-20) — Z's landing page live at `/`; email sending activated (team-only).** (1) **Landing**: `launch/from_Z/matchmaker.html` ported to `HomeLanding.tsx` and shipped as `/` (old `Home.tsx` deleted). Long-scroll marketing page; auth users auto-redirect to `/matches`; Layout hides Home-link/mobile-tabs/ChatWidget on the landing for a clean full-bleed canvas. Deferred (non-blocking): ghost-card section is still placeholder, stats ($18T/93%) unsourced, no sponsor/speaker proof. (2) **Email**: the 7 hardcoded `return # BLOCKED` guards replaced by a single `EMAIL_MODE` gate in `_send_email` — `off` (safe default) / `allowlist` / `all`. Domain-based allowlist (`@proofoftalk.io,@xventures.de` covers all team). Resend domains verified; `APP_PUBLIC_URL` default fixed to meet.proofoftalk.io. **Currently `EMAIL_MODE=allowlist` in Railway (team testing); full rollout = flip to `all`.** ✅ **VERIFIED WORKING 2026-05-20** — test reset to shaun@proofoftalk.io confirmed `status: delivered` via Resend API (from matches@proofoftalk.io). The earlier failure was a transient Railway build incident that stalled the deploy; once Railway pushed their fix, the send went through immediately. Pipeline proven end-to-end (gate → Resend → delivered). **Strategy redirect (Zohair, 2026-05-20, pending decision):** send from warm `matchmaker@xventures.de` (Resend-verified) not cold proofoftalk.io; go **transactional-only** (no mass match-intro blast — site + magic links carry it); cold-user E2E test pending. **Template redesign in flight** (`_render_email()` wrapper — light/cream/serif/wordmark; password-reset converted, 3 templates to go; 14/14 team preview delivered, awaiting reactions; code uncommitted). (3) **Data hygiene**: 5 test attendee rows + 50 ghost matches deleted from Supabase; `TEST_BUYER_NAME_PATTERNS` filter added to all 3 Rhuna writers so QA orders (Wello's `laura+NN@wello.ai`) never re-sync. New `export_companies_with_people.py` for Karl (Companies + People sheets). 679 attendees in DB.

**Prior (2026-05-19, late afternoon):** **Photo extractor v4 shipped (preload-link fallback); Sneha's photo captured.** Diagnostic on Sneha (`scripts/diagnose_photo_dom.py`) disproved the `<picture>`/`srcset`/`data-delayed-url` hypothesis — her page has 0 `<picture>` nodes and no `data-delayed-url`. Real root cause: **no `<h1>` inside `<main>` on her profile layout**, so v3's closest-common-ancestor algorithm had nothing to anchor against. Fix is additive: when the h1-anchored primary returns null, fall back to LinkedIn's `<link rel="preload" as="image" imagesrcset="…profile-displayphoto…"/>` in `<head>` — the canonical top-card avatar, h1-independent. Structurally regression-safe (fallback only fires on primary null). Smoke-tested: Sneha's `photo_url` now written to Supabase pointing at the same image visible publicly on her LinkedIn. Spot-check on Jessica Surendorff (known-working) confirmed primary path unchanged. Also kept the diagnostic script for future regressions, and added a `📷 photo_url:` print to the scraper success line. **Previous v3 (closest-common-ancestor):** picks the profile photo whose DOM path to a common ancestor with the owner's `<h1>` is shortest; still the primary path in v4 for the 94 attendees where h1 is present. **TEAM ticket_type shipped:** 15 PoT/XVentures organising staff backfilled, frontend renders Sparkles+amber-300 badge. Commits 681d061 (TEAM + v3 photo) + d93c9df (v4 queued). **Earlier:** Added `TEAM` value to the `tickettype` Postgres enum via Alembic migration `a7c4d1e8b2f5_*`; 15 PoT/XVentures organising staff backfilled (previously scattered across DELEGATE/SPEAKER/VIP); frontend renders a Sparkles+amber-300 badge for TEAM. Sneha Yadamari onboarded as the trigger (account + magic link + password + LinkedIn enrichment, photo stays NULL because her LinkedIn photo is connections-only). Photo extractor moved from section-walk to closest-common-ancestor algorithm — picks the photo whose DOM-path back to the profile owner's h1 is shortest, more robust to layout variations. Commit 681d061. **Earlier (2026-05-18 evening):** LinkedIn scraper hardened (3 correctness fixes) + first-ever discovery pass closes 30% of the URL-gap. Photo extraction now h1-anchored (no photo on LinkedIn → photo_url stays NULL; saved as `no-photo-means-no-photo` rule after operator-avatar pollution incident hit 4 attendees); headline extractor rejects post-like text via anti-post filters (fixes Maha/Nicholas/Alexis/Tony grabbing recent posts); company verification default-on for every scrape (saved as `linkedin-scrape-verification` rule — preserves rejected data under `enriched_profile.linkedin_unverified` for review). Discovery pass `--only-missing` on 243 no-URL attendees: 73 enriched / 73 URLs discovered / 170 skipped (correctly rejected wrong-person matches). Coverage: 301 fully enriched + 73 discovered URLs awaiting AI-refresh + 174 still no URL (Rhuna ticket-buyers, deferred to Concierge self-fill per `dont-chase-bad-scrapes` rule). Commits 0f262b1 + c2b662f. **Earlier today:** Three production dashboard bugs fixed and verified live. (1) `/dashboard` was painting the original Amara case-study seeds (5 attendees / 36 matches / 70% match score) as `placeholderData` on every mount — flashing for ~5s for logged-in admins, persisting forever for anonymous visitors because the four dashboard endpoints 401'd into the demo fallback. Removed both placeholders + fallbacks; render-blank-during-load is the new behaviour. (2) `/dashboard` now has a `<Navigate to="/login" replace />` guard mirroring MyMatches; anonymous hits redirect to `/login`. (3) Sign-out now wipes the React Query cache (`queryClient.clear()`) and navigates home (`navigate("/", {replace:true})`) so a signed-out admin no longer lingers on the dashboard with their last-fetched data visible. Commits e36c608 + 42a9ecf, ~50 lines total. **Previous milestone (2026-05-16):** Rhuna granular pass names preserved under `enriched_profile.extasy.ticket_name` (no migration, 362 backfilled, new "Ticket Types" dashboard card).
**Prior milestone (2026-05-13 evening):** Self-enrichment incentive #5 shipped — AI Concierge proactively drafts missing profile fields. When an attendee opens chat with < 80% completeness, a tailored welcome offers to GPT-draft the highest-priority missing field (goals → target_companies → interests). Click a candidate, edit inline, Save → embedding regenerates in the background + matches refresh. Maybe-later persists a 30-day decline cooldown so the offer rotates on the next visit.
**Stack:** Python 3.12 / FastAPI / SQLAlchemy async · React 18 / TypeScript / Vite / Tailwind · **Supabase PostgreSQL** + pgvector · OpenAI (text-embedding-3-small + gpt-4o) · **Railway** (backend) · **Resend** (email) · Netlify (frontend)

---

## What's Working

- **Priority intro requests (2026-05-29, on branch `priority-intros`)** — concierge-curated per-attendee target lists surface as a new `priority_intro` match tier above curated, with honest factual cards (no GPT fabrication) and `matches.accepted_*_at` timestamps for ROI audit. Force-include cap 50. Gold-tier sponsor perk; first client Elliptic. Backend + frontend code merged on the branch; ingest script + production push pending sheet access.
- **Reciprocity loop (2026-05-25, LIVE)** — closes one-sided-mutual. Tokenless accept-back `PATCH /m/{token}/status` so no-login attendees accept back in one tap (PR #9); Requests surface + `?tab=requests` deep-link on MagicMatches/MyMatches; backlog blast `scripts/notify_pending_interest.py` (132/132 dormant targets notified, all delivered); self-sustaining 2-hourly cron `app/services/interest_cron.py` for forward-notify + deduped mutual-completion email (PR #10), gated by env kill-switch `RECIPROCITY_NOTIFY_ENABLED` (default OFF; the real off-switch since the cron force-sends). Migrations `d4e5f6a7b8c9` + `a6dda3ac7276`. Cron activated on Railway 2026-05-25.
- **Adoption & usage tracking (2026-05-24, LIVE)** — answers "are people actually using the app?" `users.last_login_at` stamped on `POST /login`; `attendees.last_seen_at` stamped on `GET /matches/m/{token}` (the magic-link majority) — both throttled once/hour, best-effort (never break the response). Daily 03:45 UTC cron (`compute_and_upsert_usage_daily`, `sync_status` heartbeat) writes one idempotent `usage_daily` row/day, deduping people active via both paths. Admin-only `GET /dashboard/adoption` returns accounts + signup trend + live usage + day-by-day trend; "Adoption & Usage" frontend card. **Admin + demo personas are excluded from all active metrics** (shared `_active_user_filter`/`_active_attendee_filter` used by both the cron and the endpoint, mirroring `real_accounts`) so early numbers aren't inflated by operator logins or the 7 video-demo personas. Migration `c3d4e5f6a7b8`. Verified live end-to-end (PR #7 `349c4e7` + exclusion `9e38c96`).
- **Sponsor self-service invite link + universal save-trigger (2026-05-22)** — `POST /auth/join` (gated by `SPONSOR_INVITE_CODE` env var; blank = OFF) lets anyone with the shared link `https://meet.proofoftalk.io/join/<code>` self-register a full SPONSOR account, bypassing the Rhuna/Extasy ticket gate; on join `profile_pipeline.run_full_enrichment` fires detached (Grid + website → embed + matches). Every profile save — Profile page, magic-link enrichment card, AI Concierge, and registration — now fires `profile_pipeline.refresh_profile_matches` so matches refresh immediately instead of waiting for the 02:45 UTC cron.
- **Attendee photo upload + magic-link enrichment card (fixed)** — attendees upload a profile photo from their device on both the Profile page (JWT) and the magic-link page (token); browser crops/downscales to 512² JPEG, backend validates (incl. MIME-param stripping) + stores it in the public Supabase Storage `avatars` bucket and sets `photo_url`. Both endpoints rate-limited 10/min. No re-embed (photo doesn't affect matching). The no-login magic-link page now also renders its enrichment card (LinkedIn/goals/target_companies/photo) for the people it was built for: the viewer's own profile rides along on `GET /matches/m/{token}` as `MatchListResponse.viewer`, replacing the auth-gated `GET /attendees/{id}` that 401'd for no-login users and left the card permanently hidden. *(shipped 2026-05-22 via PR #2, branch `worktree-attendee-photo-upload`)*
- **Profile editing — `target_companies` persists** on the logged-in Profile page (was silently dropped pre-2026-05-22; fixed in PR #3). Saving "who you want to meet" now updates the profile + completeness score.
- **`/dashboard` properly auth-gated + honest loading state (2026-05-18)** — Anonymous hits to `meet.proofoftalk.io/dashboard` redirect to `/login` (matching the pattern at `/matches` and `/attendees`); no more case-study demo data leaking to public viewers. Logged-in admins see a brief blank-with-loader for ~1-2s while React Query fetches, then real data — no more placeholder flash of 5 attendees / 36 matches. Sign-out now clears the React Query cache and navigates to `/`, so a signed-out admin no longer lingers on the dashboard with last-fetched data still painted. Three commits today (e36c608 useDashboard.ts, 42a9ecf Dashboard.tsx + Layout.tsx). Verified end-to-end via Playwright + minified bundle inspection.
- **Rhuna granular pass-name preservation (2026-05-16)** — Rhuna-authoritative fields nested under `enriched_profile.extasy.{order_id, ticket_code, ticket_name, phone, city, country, paid_amount, voucher_code, synced_at}`. `extasy_sync.py` writes the `.extasy` sub-key with Rhuna-wins semantics on every sync (so re-runs can correct a typo without clobbering enrichment data). Runa webhook (`integration.py`) and standalone `ingest_extasy.py` write the same shape. `/dashboard/revenue` returns `ticket_types_breakdown.by_pass[]` sourced from this JSONB path; new "Ticket Types (Rhuna)" card on the Dashboard renders it as horizontal bars. Backfill at `backend/scripts/backfill_rhuna_pass_names.py` (idempotent) migrates legacy top-level keys → nested; ran once against live Supabase (362 rewrites).
- **AI Concierge proactively drafts missing profile fields (self-enrichment #5)** — Authenticated chat checks the attendee's completeness on open; if < 80% (4-or-fewer of `{goals, target_companies, interests, title, company, photo_url}` filled), it offers to GPT-draft the highest-priority missing field from `{goals → target_companies → interests}`. User picks a candidate, edits it inline, hits Save → `BackgroundTasks` reruns `MatchingEngine.process_attendee` (embedding + AI summary + ICP + intent tags) and `generate_matches_for_attendee`. Decline persists for 30 days in `enriched_profile.field_prompts.{field}`, after which the offer can fire again. SPARSE profiles (sum of {interests, goals, intent_tags, title} ≤ 1) get 2 generic candidates with a "starting points — feel free to rewrite" hint instead of 3 hallucinated specifics. `profile_data_quality()` in `concierge.py` is the single source of truth used by both the drafter and the existing `_brief_attendee_line` so the anti-hallucination posture stays in sync.
- **Free-slot visibility on match cards (Brella-style, Phase 2 #1)** — `app/services/slots.py` mirrors the 27-slot conference grid (June 2 + 3, 30-min blocks); `MatchResponse.mutual_free_slots` ships up to 4 slots free for both attendees on every mutually-accepted match with no booking yet; `MyMatches` renders an inline "Both free at — tap to book" chip strip above the full slot picker; one tap books. `PATCH /matches/{id}/schedule` returns **409 Conflict** if either party is already booked at that time; the frontend hook invalidates the cache on 409 so a stale chip disappears immediately. Surfaced only in `MyMatches` for now (admin/Briefing/MagicMatches deferred).
- **3-stage AI matching pipeline** — Embed → pgvector retrieval → GPT-4o rank & explain; **234 matches** across 85 attendees, avg score **0.713**; vertical_tags + intent_tags + target_companies + inferred_customer_profile integrated into embeddings, GPT prompt, and deterministic reranking with COMPLEMENTARY_VERTICALS boost; ML feedback loop feeds decline reasons into GPT prompt
- **`process_attendee` now classifies sector verticals (2026-05-27)** - prior to this commit `classify_verticals` only ran on the `extasy_sync` + `speakers_sync` ingest paths, so any attendee arriving via `/auth/register` (sponsor-invite, manual claim, magic-link self-fill) had empty `vertical_tags` permanently. 663/1275 attendees (52%, including 94 VIPs - Adam Back, Fabian Dori, Evin McMullen, Desmond Poon, etc.) were silently missing the COMPLEMENTARY_VERTICALS rerank boost that surfaces non-obvious cross-sector matches. `MatchingEngine.process_attendee` now always calls `classify_verticals` after `classify_intents` so future re-embeds always populate the tags. One-off backfill via `scripts/backfill_empty_vertical_tags.py` (dry-run / --confirm / --vip-only / --attendee-id / --regen-matches flags) closes the historical gap. Surfaced by Desmond Poon's "16 matches feels low" feedback - his Antler LP-fundraising profile was being matched against other VCs instead of family offices / institutional allocators because vertical_tags was empty.
- **b2b_only privacy mask in ALL LLM prompts (2026-05-27, fully shipped)** - shared `MatchingEngine._display_name` + `_mask_text_for_candidate` helpers applied across every LLM surface that ingests attendee data: (1) `rank_and_explain` candidate description block (Name->company, Title blanked, Goals + AI Summary + Grid context name-masked); (2) `rank_and_explain` decline_feedback block (counterpart label + name occurrences inside decline_reason text masked, via `_format_decline_feedback_entry`); (3) `sponsor_intelligence._generate_explanations` attendee blocks (same masking via `_describe_attendee_block`, SELECT fetches `privacy_mode`). Edge case pinned: b2b candidate with empty company uses an "Anonymous B2B attendee" sentinel rather than leaking the real name as a fallback. `_realign_entries_by_name` looks up b2b candidates by company too, so a leaked real name in `candidate_name` fails the lookup and the entry is dropped (defense in depth). Pattern for any future LLM surface: fetch `privacy_mode`, wrap as SimpleNamespace if dict, call the two helpers, pin with a "real name absent" test.
- **AI-inferred customer matching (Z's vision)** — each attendee gets a GPT-4o-inferred ICP stored in `inferred_customer_profile` JSONB: `offers`, `ideal_customers[]` (who/why/signal_keywords), `ideal_partners[]`, `anti_personas`. Fed into composite embedding text so similarity search reflects "who would buy from this person"; injected into ranking prompt with explicit weight hierarchy (EXPLICIT target_companies > AI-INFERRED ICP > BASELINE similarity); deterministic rerank adds +0.03/+0.05 when a candidate's profile contains 1/≥2 of the target's ICP signal keywords, +0.03 extra when the candidate's ICP also points back at the target (two-way fit = deal-ready). Company-similarity fallback surfaces up to 3 sector peers (shared vertical_tags or Grid sector) when no matches clear `MIN_MATCH_SCORE`, preventing empty briefings.
- **Data enrichment** — 142 attendees, all with AI summaries + embeddings + intent/vertical tags. Coverage by source: LinkedIn 66% (94/142, manual Playwright via `scripts/linkedin_scrape.py` — operator-driven, runs on demand when at the laptop); Grid B2B 27% (38/142); website ~58%. Daily cron runs Extasy → speakers → grid audit → match refresh **without LinkedIn** — manual Playwright is the only LinkedIn path now. Idempotency via `enriched_profile.{linkedin,grid,website}_enriched_at` timestamps.
- **Master speaker sheet sync (replaces 1000 Minds path)** — `backend/scripts/ingest_speakers_sheet.py` + `app/services/speakers_sheet_sync.py` pull the POT26 master Speaker Tracking Google Sheet (~144 rows: speakers, moderators, jury) and upsert into `attendees`. Bio→`goals`, LinkedIn URL→`linkedin_url`, Twitter URL→`twitter_handle`, Category="Jury*"→VIP else SPEAKER. Email cleanup splits multi-email cells and falls back to `{slug}@speaker.proofoftalk.io` when the column holds an EA/colleague address (originals captured under `enriched_profile.suspicious_email_in_sheet` for audit). Daily cron at 02:15 UTC pulls fresh from Google; admin endpoint `POST /dashboard/sync-speakers`. The old `speakers_sync.py` (read 8-row Supabase `speakers` table managed by 1000 Minds) is no longer wired into the cron — file is left in place for one cycle of safety, will be deleted next session.
- **Enhanced dashboard** — revenue tracking (€47.6k total, ticket breakdown), registration funnel (paid/failed/pending), weekly growth chart, attendee sources (Extasy/1000 Minds/self-registered), profile quality bars; live data from Extasy API with deduplication
- **Accurate "pending LinkedIn enrichment" banner (2026-05-24)** — `pending_linkedin_enrichment` now uses `_linkedin_pending` (dashboard.py), which counts only genuine actionable backlog: excludes already-enriched, `linkedin_unscrapable` (dead), already-attempted empty stubs (`linkedin_enriched_at` set, private/login-walled), and `@demo.proofoftalk.io` personas. Banner dropped from an inflated 91 to a true 10 (~8 of which are malformed URLs awaiting a deferred one-time cleanup; 2 genuinely scrapable). TDD-covered (`tests/test_linkedin_pending.py`).
- **1000minds vertical_tags** — 12 sector verticals (incl. `privacy`); display names aligned with 1000 Minds format; purple-styled tags visible on AttendeeMatches, Attendees, MyMatches; GPT responses validated against `VALID_VERTICALS`
- **The Grid B2B integration (active matching)** — `grid_enrichment.py` queries thegrid.id GraphQL API (public, no auth) by company name with retry + case-insensitive search; stores verified description, sector, products, socials in `enriched_profile["grid"]`; "Verified by The Grid" emerald card on match cards + profiles; Grid data feeds **actively** into matching: sector→vertical mapping for COMPLEMENTARY_VERTICALS scoring, products in GPT-4o candidate descriptions, product-pair boost in deterministic reranking; health check endpoint at `/dashboard/grid-health`
- **Sponsor Intelligence Reports** — admin dashboard section: select from 24 sponsors → generates personalised intelligence report (Grid → embed → pgvector → GPT-4o with overstating prevention); deterministic confidence scoring (0-1) based on data completeness; GPT forced to cite sources ([GRID], [GOALS], [PROFILE], [AI-INFERRED]) and flag sparse data; report shows relevance (HIGH/MEDIUM/LOW), conversation openers, deal potential, caveats, key evidence; CLI script also available at `backend/scripts/sponsor_intelligence.py`
- **Runa integration API** — 4 endpoints behind `X-API-Key` auth at `/api/v1/integration/*`: magic-link lookup (create-on-the-fly), ticket-purchased webhook, ticket-cancelled webhook, attendee-status check; spec doc at `docs/runa-integration-spec.md` + `.docx` for Swerve
- **AI Concierge markdown rendering** — assistant responses render with styled markdown (bold names, numbered lists, orange headers) via react-markdown; system prompt includes formatting instructions
- **Supabase synced** — full mirror of RDS: 38 attendees, 129 matches, all AI data; ready for migration cutover
- **Full attendee journey** — register (1-step form), browse matches, accept/decline with inline reason capture, mutual match confirmation, in-app messaging, meeting scheduling, ICS download, satisfaction rating
- **Role-based UI** — admin sees all attendees + matches read-only; attendees see only their own private briefing
- **POT brand design** — dark theme, `#E76315` orange, heading font, mobile-responsive (44px targets)
- **Email service** — AWS SES code shipped: new matches email, mutual match email, meeting confirmation, password reset; fire-and-forget, no-ops gracefully if unconfigured
- **Password reset** — self-service flow: forgot-password (rate-limited, no email enumeration) → SES email with 15-min JWT token → reset-password page with live validation → auto-redirect to login; no DB migration needed (stateless JWT tokens with `purpose: "reset"` claim)
- **Magic link access** — `magic_access_token` per attendee, `/m/:token` URL gives 1-click read-only match dashboard (no login required); tokens auto-generated on registration; admin bulk-generate via `POST /matches/generate-tokens`; match intro emails include magic link CTA
- **Architecture doc** — `docs/architecture-scale.md`: 3-stage pipeline scaling from 38→2,500 with pgvector IVFFlat, infrastructure upgrade path, runtime estimates
- **Cost analysis** — `docs/cost-analysis.md`: €0.39/attendee (optimised 2×/week refresh), under €0.50 target
- **Investor Heatmap** — `GET /dashboard/investor-heatmap` aggregates capital activity by sector (deploying_capital, co_investment, deal_making); horizontal bar chart + deal readiness summary on Dashboard
- **QR Business Card Exchange** — scannable QR on Profile page linking to attendee's magic link; copy link + save QR as PNG; uses react-qr-code
- **QR code in email** — match intro emails now include an inline QR code (base64 PNG) linking to the attendee's magic link dashboard
- **"Who do you want to meet?"** — new `target_companies` field on Attendee (free text); shown on Profile page + magic link enrichment card; fed into embeddings + GPT-4o ranking with highest priority per Z's direction
- **Magic link profile enrichment** — `PATCH /matches/m/{token}/profile` allows attendees to update Twitter + target_companies without login; enrichment card shown on MagicMatches page for incomplete profiles
- **Pre-Event Warm-Up Threads** — 11 auto-created vertical-based group discussion threads (tokenisation, DeFi, infrastructure, etc.); attendee's sectors highlighted and sorted first; "Threads" nav link for all authenticated users; 5s live polling
- **Social links on match cards** — LinkedIn, Twitter, and website icons on MyMatches so attendees can research their recommendations
- **Auth-aware home page** — logged-in users see "View your matches" / "Edit your profile"; logo links to /matches when authenticated
- **Saved shortlist** — bookmark per match card, All/Saved tab filter, persists in localStorage
- **Action model** — full-width filled "I'd like to meet" as dominant CTA; "Maybe later" as plain text link
- **Daily match refresh** — cron at 02:00 UTC
- **Profile photos** — user-uploaded only; AttendeeAvatar falls back to ui-avatars styled initials when no photo is set
- **Ferd outreach sheet sync (`POT Attendees` tab)** — Google Apps Script bound to `PoT26_Master_Email_Database_v3` polls Supabase via the read-only `attendees_sync` view every hour; mirrors 86 attendees into a `POT Attendees` tab plus a `POT Sync Log` tab for run metadata. Never touches `MERGED - All Investors` (which Ferd's `mergeInvestorTabs()` rebuilds on every run). Read-only anon key scoped to 6 columns; no write-back to Supabase. Repo copy of script at `docs/integrations/sheets_sync/Code.gs`.
- **Idempotent `ingest_extasy.py`** — `upsert_to_supabase()` now INSERTs new emails and PATCHes only Rhuna-authoritative fields (`extasy_order_id`, `extasy_ticket_code`, `extasy_ticket_name`, `phone_number`, `city`, `country_iso3`, `ticket_bought_at`, `ticket_type`) on existing rows via a diff-only PATCH. Enrichment data (`enriched_profile`, `ai_summary`, `embedding`, `interests`, `goals`, etc.) is never overwritten. Safe to re-run and safe to schedule.
- **`extasy_sync` service hardened (2026-04-28, awaiting deploy)** — `app/services/extasy_sync.py` rewritten with per-row Postgres SAVEPOINT (`db.begin_nested()`) so a single bad row can't poison the whole batch (was causing every nightly run to insert zero rows). Also now backfills `extasy_order_id` + `country_iso3` + merges Extasy fields into `enriched_profile` on every existing-row match, even when the ticket type isn't an upgrade (was silently skipping). New `backfilled` counter in stats.
- **Ticket holder export** — `backend/scripts/export_ticket_holders.py` produces `exports/ticket_holders_company_position.csv` (gitignored) for outreach lists. Pulls everyone with `extasy_order_id IS NOT NULL` from Supabase; uses `enriched_profile.linkedin.headline` as the position field when registration title is empty.

## Infrastructure

- **Production URL**: `https://meet.proofoftalk.io` (Netlify frontend → Railway backend)
- **Supabase Storage `avatars` bucket** (public-read) on project `mkcememoueziibbpqhfk` — stores attendee profile photos; all writes go through the backend service-role key (RLS bypassed). Requires `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` env vars (now in `Settings`).
- **Backend**: **Railway** (`proofoftalkcd-production.up.railway.app`) — x-ventures Pro plan, auto-deploys from GitHub `main` branch, root dir `backend`
- **Database**: **Supabase PostgreSQL** (`db.mkcememoueziibbpqhfk.supabase.co:6543/postgres` - **dedicated transaction pooler**) - XLabs Ext Pro plan, IPv4 add-on, shared with 1000 Minds (`speakers` table). Engine hardened 2026-05-21 (pre_ping + recycle + env-tunable pool) and 2026-05-28 (both `statement_cache_size=0` AND `prepared_statement_cache_size=0` auto-set when DSN port is `:6543` - needed because pgbouncer transaction mode multiplexes server connections across client transactions, so any cached prepared statement leaks across connections and fails with `__asyncpg_stmt_X__ does not exist`). **The direct port-5432 endpoint is dead** for this project as of 2026-05-28 - Supabase forced the migration to the pooler without warning while the IPv4 add-on stayed active (silent change, no email). Both local `.env` and Railway `DATABASE_URL` must use `:6543`.
- **Email**: **Resend** — sending from `Proof of Talk <team@xventures.de>` (warm domain; Reply-To `team@xventures.de`). Gated by a single `EMAIL_MODE` switch in `_send_email`: `off`/`allowlist`/`all`. **Currently `allowlist`** (`@proofoftalk.io,@xventures.de` — team only) so automated *engagement* triggers (match intro, mutual) reach only the team. **Account-recovery sends bypass the gate via `force=True` — both the unclaimed-attendee welcome (forgot-password) AND the password-reset for claimed accounts (fixed 2026-05-22 `0c47cf8`; previously a claimed non-team account, e.g. `@xapo.com`, could never reset its password while allowlist held).** The staged welcome batch also forces per-send. Full rollout = flip `EMAIL_MODE=all` in Railway (no redeploy). 5 templates share the newsletter-styled `_render_email` wrapper.
- **EC2 decommissioned**: was on personal AWS account; no longer in use
- **RDS stopped**: `pot-matchmaker` instance stopped 2026-04-15 with final snapshot `pot-matchmaker-preretire-20260415-0757`. AWS auto-restarts stopped instances after 7 days — consider deleting with final snapshot if not needed.
- **Deploy**: push to `main` → Railway auto-deploys backend; Netlify auto-deploys frontend (GitHub App relinked 2026-04-15)
- **Background jobs**: long-running admin actions (Grid re-enrich, sponsor reports) run as asyncio background tasks with `GET /dashboard/jobs/{id}` polling — no more 504 timeouts

## Broken / Incomplete

- **Login onboarding for unclaimed attendees** — ~714 of 799 attendees have a magic token but no login account. Addressed 2026-05-22 (PR #3, merged): `/login` is now **passwordless-first** — an always-visible primary "Email me a sign-in link" (reuses enumeration-safe `forgot-password`; unclaimed attendees get their `/m/{token}` magic link), with password login kept but de-emphasized below a divider. Browser smoke-tested. Still relies on the attendee acting on the email; bulk welcome-email rollout is ongoing (see whats_next). Deferred: a true one-click JWT magic-login for existing password accounts (existing accounts currently get a reset link).
- **[deeper-match-pool] LIVE 2026-05-21 17:04** — curated+deep tiered match pool, enrichment-gated visibility cap (5/10/20), "Maybe later" soft-defer. Merged to main, migration `b2c3d4e5f6a7` applied, matches regenerated (curated 2248 + deep 1223), Railway deployed `39594090`/`94172f0` (defer route verified at `/api/v1/matches/{id}/defer`), frontend Netlify `707c481`. Only the in-browser UI smoke-test remains (API-level deployment confirmed). Moved here from Broken once deploy verified.
- **Daily-sync fixes shipped locally, awaiting Railway deploy** — Three issues fixed and smoke-tested 2026-04-30: (a) added 02:45 UTC `_daily_match_refresh()` (was missing despite docs); (b) declared `ticket_bought_at` on the ORM and wired into `extasy_sync` (128/128 coverage now); (c) refactored `grid_audit` off Supabase REST → SQLAlchemy (eliminates 401 from key rotation). Local Supabase already reflects the fixes via smoke runs. Until next push, Railway will continue running yesterday's code, and tomorrow's 02:00 cycle will produce the same gaps as today.
- **Reassigned ticket holders not in matchmaker (decided 2026-04-28)** — 23 reassigned Extasy tickets exist where buyers purchased extra passes for colleagues. We confirmed the data quality is too poor to auto-create rows (`ticketOwners` is sparse: "Journalist 1", first names only, no email/company/LinkedIn). **Decision**: don't auto-create skeleton rows that would pollute the attendees list with un-matchable ghosts. Path forward = either a buyer-facing magic-link flow ("name your colleagues") OR colleague self-registration. Until built, those ~23 attendees won't get matches.
- ~~**Dashboard has no `last_extasy_sync_at` indicator**~~ — shipped 2026-05-17 as the **Sync Health panel** covering all 7 crons with colour-coded age and status badges. First view surfaced the `daily_speakers_sync` 401 (separate fix pending).
- **Email template design** — match intro email is functional but needs design polish for production use (layout, branding, content)
- **Grid coverage ceiling** — 23/85 attendees (27%) verified by The Grid. Remaining 62 are companies genuinely not indexed by Grid (confirmed via name + URL + email-domain probes). Would need Grid to expand their index or manual canonical-name dict for edge cases.
- **Attendee onboarding flow** — attendees like Pouneh Bligaard have Rhuna tickets but no user accounts on the platform. No self-serve path to get matches until emails are re-enabled or magic links are distributed.
- **Full email HTML templates** — morning schedule, D+1 wrap-up, D+7 nudge have function stubs but no HTML body yet (return-at-top blocks execution). Templates to be written when emails are re-enabled.

## Attendee Experience Phases (from docs/matchmaking-ux-integration.md)

| Phase | Status |
|---|---|
| 1. Instant (post-purchase magic link) | Blocked on Rhuna webhook go-live |
| 2. First Matches (24-48h email) | Blocked on Phase 1 |
| 3. Warm-Up (threads, messaging, scheduling) | Built |
| 4. Final Briefing (prep brief + PDF) | **Shipped** — `/m/:token/briefing` |
| 5. At-Event (morning email, QR, feedback) | Easy parts done, real-time = stretch |
| 6. Post-Event (contact export, follow-up) | **Shipped** — CSV export + email stubs |

## Key Decisions Made

- **pgvector + GPT-4o hybrid** (not pure LLM) — pure LLM over N² pairs is too slow and expensive at 2,500 attendees; pgvector retrieves top-K candidates cheaply, GPT-4o only scores those — cost-effective without sacrificing match quality
- **Per-party status (status_a / status_b)** instead of single match status — enables two-sided consent UX; each attendee independently accepts/declines; correct mutual detection without extra tables
- **MIN_MATCH_SCORE = 0.60** — anything below is filtered before persisting; avoids padding attendees with weak connections that dilute the briefing
- **No automatic profile photo fetching** — GDPR compliance; the platform does not pull or store photos from LinkedIn or any third-party source; users upload their own photo URL if they choose to
- **Fire-and-forget email** — SES calls never raise to the caller; the product works without email, email is an enhancement layer; avoids adding failure modes to the match pipeline
- **localStorage for saved shortlist** — no backend change needed for demo; fast to ship, sufficient for a product demo at this stage

## Deployment

- **Production URL**: `https://meet.proofoftalk.io` (Netlify frontend → Railway backend → Supabase)
- **Deploy**: push to `main` → Railway auto-deploys backend, Netlify auto-deploys frontend (GitHub App relinked 2026-04-15)
- **EC2 + RDS**: both decommissioned/stopped. RDS snapshot: `pot-matchmaker-preretire-20260415-0757`

## Current Numbers (2026-04-28)

- **107 ticket holders** in Supabase with `extasy_order_id` properly linked (was 81 before today's backfill)
- **116 attendees** total (ticket holders + speakers + nominees)
- **234 matches** at avg score 0.713 (last full re-rank: 2026-04-24)
- **Rhuna/Extasy**: 253 total orders, 138 valid PAID/REDEEMED, 132 non-test, 107 unique buyer emails, ~€71.5k revenue
- **All emails disabled** until platform opens to attendees
- **Daily Extasy sync** — fires at 02:00 UTC on Railway but currently insert-zero due to bugs being fixed. Local fix verified, awaiting deploy.

## Current Focus

- **Ship the extasy_sync fix** (uncommitted on `main`) → verify Railway → change cron to every 5h
- **Add `last_extasy_sync_at` indicator** to admin dashboard so silent drift is detectable
- Re-enable emails + attendee onboarding when platform is ready for attendees to sign in
- Sponsor intelligence rollout — Victor pitching pilot reports
- Align CEO dashboard with matchmaker dashboard revenue figures

## 2026-05-25 — User-editable AI write-up (LIVE)
- Attendees can edit their own AI write-up (`ai_summary`) on the Profile page and/or Regenerate it from AI. A user edit sets `attendees.ai_summary_pinned=true`; `MatchingEngine.process_attendee` skips `generate_ai_summary` when pinned, so the user's text survives every re-embed/cron and feeds their match embedding. Empty submission un-pins ("reset to AI"). 2000-char cap. On for all attendees. Magic-link self-fill card NOT covered (v1 scope).

## 2026-05-29 — Concurrency hardening (PR #19 LIVE)
- `matches` table now has `uq_matches_pair` unique index on `(LEAST(a,b), GREATEST(a,b))` blocking dup pairs at DB level. Migration `c4f1a2e8b3d7`.
- All 3 `Match()` insert sites in `services/matching.py` (`_persist_ranked`, `_apply_priority_intros` force-add, sector-fallback) now savepoint-wrapped with `IntegrityError`-catch race-guard.
- `refresh_profile_matches` errors UPSERT to `sync_status.refresh_profile_matches` (no longer silently swallowed in logs only).
- Per-attendee `asyncio.Lock` in `refresh_profile_matches` (WeakValueDictionary keyed by attendee_id) — concurrent saves for same person serialize; different attendees still parallel.
- Pre-cleanup audit: 39 dup pairs in prod. Post-cleanup: 0.
- Tests: 378 passed (+12 vs yesterday's 366).
- Ylli duplicate attendee row (`ylli.vllasolli@elliptic.co`) deleted (29 throwaway matches + 1 attendee row, atomically).

## 2026-05-29 — Elliptic priority intros LIVE (PRs #17 + #18)
- 60 `RequestedIntro` rows persisted (Aylin 33 + Ylli 27); 28 resolved to existing attendees.
- Aylin: 16 `priority_intro` Match rows. Ylli: 11 (was 0 before today's `_apply_priority_intros` commit() fix — `flush()`-only was being rolled back at session `__aexit__`).
- New tier surfaces above curated on both `/matches` (authed) and `/m/{token}` (magic link) for both Elliptic accounts.
- Unresolved intros (32) show as greyed-out cards; auto-resolve as targets register via Rhuna.
- Migration `9b3e2d1a8c4f` at prod alembic head (now superseded by `c4f1a2e8b3d7`).

## 2026-05-30 update
- Same-org matching filter live (`same_org_filter.py`): no more "matched with CEO of my own company" reports.
- Full refresh in progress: 1300/1606 done at 09:21, rate sped up ~30x overnight (likely OpenAI throttle cleared), ETA ~2.5 min for the remainder.
- `scripts/full_refresh_matches.py` now wraps each attendee in `asyncio.wait_for(timeout=180)` + accepts a resume-from-index argv to recover from mid-run kills.
